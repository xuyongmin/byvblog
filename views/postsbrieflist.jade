extends layout
include includes/pagination

block title
  title= label('List of posts') + ' - BYVoid'

block banner-image
  img.intro-img(src="/images/banner-bg-1.jpg")

block content
  h1= label('List of posts')
  #main.postlist(role="main")
    table
      for post in posts
        tr
          td= dateText(post.postTime)
          td
            a(href=langLink('/' + post.id), data-post-id=post.id)= post.title
    mixin pagination(postId)

block scripts
  script.
    history.replaceState('postbrieflist');
